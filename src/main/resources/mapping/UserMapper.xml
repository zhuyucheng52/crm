<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.crm.mapper.UserMapper">
    <select id="selectByKeyword" resultType="com.echo.crm.entry.User">
        select id, name, sex, birthday, mobile, address, email, remark, account, user_id
        from tbl_user
        where disabled = 0
        <if test="key != null and key != ''">
            and (id = #{key}
                or account = #{key}
                or name like concat('%', #{key},'%')
                or mobile like concat('%', #{key},'%')
            )
        </if>
        order by id asc
    </select>
    <select id="selectByAccount" resultType="com.echo.crm.entry.User">
       select *
       from tbl_user
       where account = #{account}
    </select>
</mapper>
