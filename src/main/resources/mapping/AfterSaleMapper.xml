<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.echo.crm.mapper.AfterSaleMapper">
    <select id="selectByKeyword" resultType="com.echo.crm.entry.AfterSale">
        select *
        from tbl_after_sale
        where disabled = 0
        <if test="key != null and key != ''">
            and (
              name like concat('%', #{key}, '%')
              or order_id = #{key}
              or id = #{key}
            )
        </if>
        order by id desc
    </select>
</mapper>
